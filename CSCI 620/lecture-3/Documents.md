
## Dataformat
MongoDB is based on JSON or lightweight data-interchange, but supports BSON types which is more types than JSON supports.

## Sample Document
- document has "fields" not "attributes"
- fields can also be another objects or lists
- object keys needs to be unique within that objects
- 