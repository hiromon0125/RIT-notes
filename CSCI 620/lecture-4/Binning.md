
## Idea
 We create a lot of buckets of equal size or depth

- Equal Depth
	- Each bucket has the same value
- Equal Width
	- Each bucket has the same range
	- calculation for each bucket id should be FLOOR(((x-x_min)*k)/width)